define(["jquery", "q", "firebase"], function($, Q, firebase) {

/*	return function(hitEnter) {

		 $(document).keyup(function (e) {
	        var key = e.which || e.keyCode;
	            if (key === 13) {
	            hitEnter.click();
	          }
		 });

	};*/
});